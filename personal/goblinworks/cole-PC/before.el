
;; They seem to be using a tab-width of 4
(setq kooru/tab-width 4)
(setq kooru/fill-column 80)

;; Add gnuwin utils to PATH.
;; This is so diff and find can work.
;;(setenv "PATH" (concat "C:/util/gnuwin/bin;"
;;                       "C:/msys/1.0/bin;"
;;                       (getenv "PATH")))
;;
